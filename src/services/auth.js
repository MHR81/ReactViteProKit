import requests from "./api/base-api";

const authService = {
//   login: (credentials) => requests.post("/auth/login", credentials),
//   register: (userInfo) => requests.post("/auth/register", userInfo),
//   logout: () => requests.post("/auth/logout"),
};